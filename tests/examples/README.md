# Test examples

These larger programs exercise multiple engine systems and provide realistic
fixtures for tests. They are maintained only to the extent required by those
tests; they are not polished public examples or part of the eventual manual.

A program belongs here when tests import its behaviour but its presentation and
API usage have not been deliberately tuned as teaching material. After that
tune-up it may be promoted to the repository-level `examples/` directory.
